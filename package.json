{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "dev:app": "pnpm --filter app dev",
    "dev:all": "pnpm --parallel --filter web --filter server dev",
    "build": "pnpm --filter web build",
    "build:server": "pnpm --filter server build",
    "db:generate": "pnpm --filter server db:generate",
    "start": "pnpm --filter web start",
    "start:server": "pnpm --filter server start",
    "start:app": "pnpm --filter app start",
    "lint": "pnpm --filter web lint",
    "lint:server": "pnpm --filter server lint",
    "lint:app": "pnpm --filter app lint",
    "app:ios": "pnpm --filter app ios",
    "app:android": "pnpm --filter app android",
    "app:build:dev:android": "cd apps/app && eas build --profile development --platform android",
    "app:build:dev:ios": "cd apps/app && eas build --profile development --platform ios",
    "app:build:preview:android": "cd apps/app && eas build --profile preview --platform android",
    "app:build:preview:ios": "cd apps/app && eas build --profile preview --platform ios",
    "app:build:android": "cd apps/app && eas build --profile production --platform android",
    "app:build:ios": "cd apps/app && eas build --profile production --platform ios",
    "app:build:all": "cd apps/app && eas build --profile production --platform all",
    "app:submit:android": "cd apps/app && eas submit --platform android",
    "app:submit:ios": "cd apps/app && eas submit --platform ios",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@eslint/eslintrc": "^3.3.3",
    "eslint-config-next": "^16.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "pnpm": "^8.0.0",
    "prettier": "^3.7.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
